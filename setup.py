from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The project demonstrates how DVC automatically tracks parameters and metrics in experiment tracking when working with pipelines',
    author='Himanshu',
    license='MIT',
)
